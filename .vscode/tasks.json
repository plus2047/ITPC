{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run C++ / Java / Python3",
            "type": "shell",
            "command": "${workspaceFolder}/quickrun.py",
            "args": [
                "-s",
                "${relativeFile}",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}